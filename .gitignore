# —— Build output (no website artifacts to GitHub) ——
dist/
.output/
.vercel/
.netlify/

# —— Dependencies ——
node_modules/

# —— Astro cache (temporary) ——
.astro/

# —— Rust build (engine-rust and any other Cargo projects) ——
target/
**/target/

# —— macOS ——
.DS_Store
.AppleDouble
.LSOverride

# —— Logs and temporary output ——
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
test-output.log

# —— Environment (never commit secrets) ——
.env
.env.local
.env.*.local
.env.production
ip_research_agent/.env

# —— IDE / editor ——
.vscode/
.idea/
*.swp
*.swo
*~

# —— Generated / local ——
audit-report-*.csv
technical_spec*.html

# —— Internal / confidential (not for public repo) ——
internal-docs/
STYLE_GUIDE.md
TECHNICAL_SPECIFICATION_IDEPOSIT*.md
docs/specs/TECHNICAL_SPECIFICATION_IDEPOSIT*.md
notities_taco/
