---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Beta Pioneer Program">
  <div class="bg-gray-50 min-h-screen">
    <main class="container mx-auto px-4 py-12 max-w-xl">
      <h1 class="text-3xl font-bold text-gray-900 mb-2">Join the Beta Pioneer Program</h1>
      <p class="text-gray-600 mb-8">
        Vraag API-toegang aan voor early access. We kwalificeren leads op use-case en geven je een API key voor de Legal Guardrail.
      </p>

      <form
        class="bg-white rounded-xl shadow-lg border p-6 mb-8"
        action="#"
        method="post"
        id="pioneer-form"
      >
        <p class="text-sm text-amber-800 bg-amber-50 border border-amber-200 rounded px-3 py-2 mb-6">
          <strong>Beta – inspanningsverplichting:</strong> De API wordt aangeboden op basis van best effort; geen resultaatgarantie of verzekering tegen boetes. Door aan te vragen ga je hiermee akkoord.
        </p>
        <div class="space-y-4">
          <div>
            <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Naam</label>
            <input type="text" id="name" name="name" required class="w-full rounded border border-gray-300 px-3 py-2" placeholder="Jan Jansen" />
          </div>
          <div>
            <label for="email" class="block text-sm font-medium text-gray-700 mb-1">E-mail</label>
            <input type="email" id="email" name="email" required class="w-full rounded border border-gray-300 px-3 py-2" placeholder="jan@bedrijf.nl" />
          </div>
          <div>
            <label for="company" class="block text-sm font-medium text-gray-700 mb-1">Bedrijf (optioneel)</label>
            <input type="text" id="company" name="company" class="w-full rounded border border-gray-300 px-3 py-2" placeholder="Bedrijf B.V." />
          </div>
          <div>
            <label for="usecase" class="block text-sm font-medium text-gray-700 mb-1">Wat is je primaire use-case?</label>
            <textarea id="usecase" name="usecase" rows="3" required class="w-full rounded border border-gray-300 px-3 py-2" placeholder="Bijv. Customer Support, Internal Agents, HR Filtering, Copilot-verkeer op het netwerk laten checken..."></textarea>
          </div>
        </div>
        <div class="mt-6">
          <button type="submit" class="w-full inline-flex justify-center items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700">
            Aanvraag versturen
          </button>
        </div>
      </form>

      <p class="text-sm text-gray-500 mt-4">
        Bij verzenden opent je e-mailclient met een vooringevulde mail. Koppel later aan Formspree of eigen endpoint om aanvragen direct te ontvangen.
      </p>
    </main>
  </div>
</Layout>

<script>
  document.getElementById('pioneer-form')?.addEventListener('submit', (e) => {
    e.preventDefault();
    const form = e.target as HTMLFormElement;
    const data = new FormData(form);
    const mailto = `mailto:contact@compliancecode.eu?subject=Beta Pioneer aanvraag: ${encodeURIComponent(String(data.get('company') || data.get('email')))}&body=${encodeURIComponent(
      `Naam: ${data.get('name')}\nE-mail: ${data.get('email')}\nBedrijf: ${data.get('company')}\nUse-case: ${data.get('usecase')}`
    )}`;
    window.location.href = mailto;
  });
</script>
